#include "main.h"

/**
 * mul - multiplies two integers a and b
 * @a: one of the two integers
 * @b: the other integer
 * Return: the product of the two integers.
 */

int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}

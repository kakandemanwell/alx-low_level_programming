#include "main.h"

/**
* add - adds two integers and returns the result
* @a: int to add
* @b: int to add
* Return: the results of the addition.
*/

int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
